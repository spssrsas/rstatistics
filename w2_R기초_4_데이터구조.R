#--------------------------------------------------------------------------
# 화일명 : w2_R기초_4_데이터구조.R
# 설  명 : 통계 데이터 분석 (R기초 - 데이터구조)
# 일  자 : 2020.11.06
# 생성자 : Jigwan
# 패키지 : 
# 분석방법: 
#--------------------------------------------------------------------------

#--------------------------------------------------------------------------
# 0. 환경 설정
#--------------------------------------------------------------------------
## 작업 디렉토리 설정
#setwd("C:/work/RProject/rstatistics")
getwd()


#--------------------------------------------------------------------------
# 데이터 구조 (개념-이미지)
#--------------------------------------------------------------------------
imgPath <- "image/dataStructure.png"
source("rasterImg.R")


#--------------------------------------------------------------------------
# 데이터 구조
#--------------------------------------------------------------------------
# 1. 벡터
vect <- c(1,2,3,4,5)

# 2. 펙터 (카테고리:범주형 데이터)
grage <- c("A", "B", "C", "A", "B")
factor(grage)

# 3. 행렬
mt <- matrix(1:12, 3, 4)
mt

# 4. 배열
ar <- array(1:12, c(2,3,2))
ar

# 5. 데이터프레임
dt <- data.frame(product=c("A", "B", "C"), price=c(100, 200, 300))
dt

# 6. 리스트
lst <- list(x=vect, y=mt, z=dt)
lst
